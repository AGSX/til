# Debugging Network Using iperf

```
# Server host

$ iperf -s

# Client host

$ iperf -c <iperf_server_ip>
```

This would run packets through both hosts and you get the result of the network speed between them.
